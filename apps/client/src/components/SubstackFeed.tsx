// SubstackFeed.tsx
// Placeholder for Substack RSS integration.
// When the Substack RSS feed is accessible, replace this component with a live feed fetch.
// Substack RSS URL: https://substack.com/@priya289311/feed
// Each item should display: title, date, readTime, excerpt, "Read Essay -->" link
// Current state: renders nothing; the existing PostCard grid handles article display.

const SubstackFeed = (): null => {
  // RSS integration placeholder — connect when Substack RSS is publicly accessible
  return null;
};

export default SubstackFeed;
